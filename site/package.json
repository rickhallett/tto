{
  "name": "tto-site",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run server.ts",
    "dev": "bun --watch run server.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.4.2"
  }
}
